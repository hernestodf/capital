# Bloqueia acesso HTTP a este diretório (contém credenciais e dados internos)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
